import React from "react";
import Card from "@mui/material/Card";
import { Typography } from "@mui/material";

export default function ListingCard(props: any) {
  return (
    <Card variant="outlined">
      <Typography>Listing Card</Typography>
      <Typography>Listing Card</Typography>
      <Typography>Listing Card</Typography>
    </Card>
  );
}
